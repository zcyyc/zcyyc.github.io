# Chenyu Zhao's Personal Website

Static GitHub Pages site for Chenyu Zhao. Open `index.html` locally to preview.

To publish, create or use the repository `zcyyc.github.io`, set it as the `origin` remote, push the `main` branch, then enable **Settings → Pages → Deploy from a branch → main / root**.
